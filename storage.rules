rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // By default, allow all authenticated users to read and write.
    // This is not secure. You should restrict access based on your app's needs.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
